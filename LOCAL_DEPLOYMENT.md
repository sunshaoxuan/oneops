# 本机 HTTPS 与 WebDAV 入口

更新时间：2026-07-24

## 对外入口

| 地址 | 用途 | 认证 |
| --- | --- | --- |
| `https://192.168.20.54/` | OneHR 保守运维工作中心 | OneOps 用户会话 |
| `https://192.168.20.54/api/work-center/v1/` | 工作中心 API | OneOps 用户会话与 RBAC |
| `https://192.168.20.54/webdav/` | `D:\root` WebDAV | 仅 `tstokyo` |
| `https://192.168.20.54/admin-webdav/` | `D:\` WebDAV | 仅 `Administrator` |

## 内部转发

| Nginx 路径 | IIS 后端 |
| --- | --- |
| `/api/work-center/v1/` | `http://127.0.0.1:8092` |
| `/webdav` | `http://127.0.0.1:8443/webdav` |
| `/admin-webdav` | `http://127.0.0.2:8443/admin-webdav` |

两个 IIS 后端仅监听回环地址。外部客户端统一连接 Nginx 的 443 端口。

工作中心前端文件部署在 `D:\nginx\html`。产品构筑源码、Python 运行时、固定模板、构筑历史和交付结果全部位于 `D:\nginx`。网关通过标准输入输出协议管理内部构筑 worker，每两秒从内部构筑功能聚合任务并从远端构建终端读取系统资源。内部 worker 不监听 TCP 端口，原 `8091` 服务已经退出。SSE 代理已关闭响应缓冲。

用户、外部身份、会话、角色、权限、用户角色分配、一次性 SSO 票据和认证审计保存在本机业务数据库中。首次注册用户完成系统管理员引导，后续本地注册用户进入待审核状态。全部工作中心业务 API 已启用会话认证、RBAC 和修改请求 CSRF 校验。

Windows SSO 复用 OHR0067 上既有的 EnvPortal 域认证。浏览器从 EnvPortal 取得短期身份令牌并通过表单提交给 OneOps，OneOps 服务端向 EnvPortal 验证令牌和用户档案后，在自己的数据库中完成用户建档、会话签发和 `VIEWER` 角色分配。

启用 Windows SSO 时，在 `D:\nginx\app\.env.local` 增加：

```text
OPS_PUBLIC_BASE_URL=https://192.168.20.54
OPS_ENVPORTAL_SSO_URL=http://OHR0067:8998/oneops_sso.jsp
OPS_ENVPORTAL_PROFILE_URL=http://192.168.20.38:8999/auth_windows.jsp
OPS_SSO_ALLOWED_DOMAINS=onehr.jp
OPS_SSO_AUTO_LOGIN=true
OPS_SESSION_TTL_SECONDS=28800
```

配置生效后，未登录用户每个浏览器标签页自动尝试一次 Windows SSO。EnvPortal 返回的电子邮件必须精确属于 `onehr.jp`。首次 SSO 自动建档为 `ACTIVE` 和系统范围 `VIEWER`。本地注册、登录、会话、用户管理和 RBAC 保持启用。

组织机构档案通过同一接口前缀访问本机数据库。机构表使用隐藏物理 ID 作为主键，`code` 是业务唯一键，`name` 是业务信息。普通画面仅显示业务字段。档案间强关联必须使用物理 ID 作为外键。

组织机构区分保存在独立的 `organization_classifications` 系统档案表中。组织机构通过 `classification_id` 物理外键引用区分档案，组织机构表不保存区分名称文本。数据源中的新区分名称会先补充区分档案，再建立物理 ID 引用。

“管理”模块提供统一的“基本档案管理”入口。当前可维护组织区分档案，提供查询、新增和编辑功能；后续基本档案通过同一目录和页面框架接入。

组织机构数据源登记在 `D:\nginx\app\config\system.config.json`。数据源 1 指向 `D:\nginx\refs\★各機関情報一覧*20250530*サポート確認.xlsx`，固定读取工作表 `機関別導入製品一覧 (サポート確認後)`。同步机制新增缺失档案，并只补充现有档案的空字段，不覆盖或删除已有信息。同一业务代码出现不同名称时发送系统消息的功能已记录为 TODO。

不带末尾斜杠的 `/webdav` 和 `/admin-webdav` 会由 Nginx 跳转到对应的 HTTPS 目录地址，避免 IIS 回环后端生成 HTTP 跳转。

IIS 后端使用回环 HTTP，由 Nginx 在 443 终止 TLS。原有的 `Administrator IIS WebDAV HTTPS 8443` 入站防火墙规则已停用。

## TLS 证书

证书主题为 `CN=192.168.20.54`，SAN 包含：

* `IP Address=192.168.20.54`
* `DNS Name=TS2DEVSERVER`
* `DNS Name=localhost`

证书指纹：`27D535B6FA15B8B922D83E890765F7F0FE76EBF9`

有效期：2026-07-23 至 2031-07-23

证书文件位于 `D:\nginx\conf\certs`。Nginx 配置中的证书路径相对于 `conf` 目录。公钥证书已加入本机受信任根证书颁发机构。其他客户端需要单独信任 `internal-site.crt`。

供 Windows 客户端导入的 DER 格式公钥证书：

`https://192.168.20.54/internal-site.cer`

客户端应将该证书导入“受信任的根证书颁发机构”。预期指纹为 `27D535B6FA15B8B922D83E890765F7F0FE76EBF9`。

## 自动启动

计划任务 `Nginx HTTPS Gateway` 在系统启动时以 `SYSTEM` 身份运行 Nginx。任务已通过停止现有进程并重新启动的方式验证。

计划任务 `OneHR Operations Compat Gateway` 在系统启动时以 `SYSTEM` 身份运行 `D:\nginx\app\gateway\server.mjs`，仅监听 `127.0.0.1:8092`。Node.js 运行时位于 `D:\nginx\runtime\node`。

同一网关进程启动 `D:\nginx\app\builder\oneops_worker.py`。Python 运行时位于 `D:\nginx\runtime\python`。构筑 worker 只使用标准输入输出与网关通信，不监听 `8091` 或其他 TCP 端口。

机构数据库容器 `onehr-operations-postgres` 使用官方 `postgres:18.4-trixie` 镜像，只绑定 `127.0.0.1:55433`。数据保存在外部命名卷 `onehr-operations-postgres-data`，重建容器时保留。兼容网关同时在系统启动和用户登录时触发，依赖尚未就绪时按一分钟间隔重试。

## 已执行检查

* `nginx.exe -t` 配置检查通过
* HTTPS 首页返回 `200 OK`
* 工作中心 Dashboard 返回 `200 OK`，上游状态为 `UP`
* 工作中心 SSE 在五秒测试窗口内持续返回 `snapshot` 事件
* 工作中心响应未发现密码、Token、凭据或秘密字段
* 组织机构新增、修改和查询接口通过
* 组织机构表物理 ID 主键与业务代码唯一约束通过
* 初次迁移生成 8 条区分系统档案，均具有独立物理 ID、唯一业务代码和名称
* 原组织机构区分文本列已移除，`classification_id` 外键约束通过
* 105 条组织机构中 89 条已关联区分物理 ID，原数据未提供区分的 16 条保持空值
* 初次迁移验证时网关重复启动后区分档案仍为 8 条，组织机构物理 ID 与业务代码指纹保持不变
* 组织机构编辑画面的区分字段已改为档案选择控件，当前可读取 9 条区分档案
* 组织机构台账标题栏已移除无业务含义的数据库运行状态标签
* 组织机构维护画面已隐藏“共通上下文”机关选择器，浏览器确认台账总数仍为 105 条
* 组织机构及基本档案表头和正文已增大字号；业务 Code 为 16 像素无边框纯文本
* 组织机构台账首次进入按 Code 升序排列；切换为机关名升序后刷新仍保持用户排序
* 保守有无已限制为 `〇`、`✕` 或空值，前端自由输入已替换为选择控件，数据库检查约束已启用
* 组织机构新增备注字段；文化庁状态为 `〇`，备注保留“保守有無原文：文部科学本省と一緒”
* 组织机构显示 100 条并滚动到底部后切换到“环境”，页面滚动位置恢复为 0，720 像素视口下无多余纵向滚动条
* 原“相談アシスタント”已统一更名为“問合支援”，导航、工作台入口和功能页标题已在浏览器确认
* “管理”中的“基本档案管理”页面已显示当前 9 条组织区分档案，且不显示组织机关上下文
* 组织区分新增和编辑接口通过临时记录验证，编辑前后物理 ID 保持不变，临时记录已清理
* 组织区分新增与编辑弹窗已在浏览器确认，物理 ID 未显示在普通画面
* 组织机构五个业务字段的表头排序通过，机关代码升序结果从 `0001` 开始
* 组织机构表头列宽可调整，并在刷新后保留调整结果
* 组织机构分页支持每页 20、50、100 条，默认 20 条；切换至 50 条后实际显示 50 行，刷新后仍保留
* 操作列按一个编辑图标固定为 56 像素，画面右侧持续可见
* `refs` 数据源确认后工作表读取 105 行，新增 99 条，校正 4 条 `LEGACY-` 业务代码，最终机构档案 105 条
* 既有 6 条档案的物理 ID 在数据源同步前后保持不变
* Code `0001` 的同 Code 异名冲突已写入结构化日志，未覆盖现有档案
* PostgreSQL 容器重启后机构记录数量保持不变，网关自动恢复为 `UP`
* 指纹化前端资源返回一年期 immutable 缓存策略
* `index.html` 使用 `no-cache`，部署新版本后浏览器会重新验证入口文件
* 认证与 RBAC 数据库迁移连续执行两次成功，预置 3 个角色和 12 项权限
* 未登录访问 Dashboard 返回 `401`，认证配置和会话状态接口可公开读取
* 首位用户引导注册返回 `201`，签发安全会话后可读取系统管理员用户管理接口
* 缺失 CSRF Token 的修改请求返回 `403`，带会话绑定 Token 的退出请求返回 `200`
* 浏览器完成首次管理员注册、工作台登录、用户台账、角色授权和认证审计验证
* 预置角色名称、说明、权限名称和审计事件在日文、中文、英文界面按当前语言显示
* 退出登录使用独立无障碍图标按钮，浏览器验证后返回登录页，服务端会话已撤销
* 390×844 视口登录页宽度为 390 像素，无横向溢出，页面高度适配视口
* 浏览器控制台未发现错误或警告
* “製品構築”已通过 OneOps 同源路径加载，浏览器未访问 `8091`
* 共通コンテキスト选择“筑波大学”后，构筑画面的机关名称自动填充为“筑波大学”
* 机关名称已通过“筑波大学 手動確認”临时值验证可编辑，并恢复为“筑波大学”
* 构筑终端显示“稼働中”，CPU、空闲内存和空闲磁盘数据可读取
* 原 13 条成功任务和 14 个正式交付目录已迁入 OneOps，任务成果物清单完整
* 原 `OHRStandaloneConsole` 服务已删除，监听检查只保留 `443` 和本机 `8092`
* 产品构筑生产页面完成浏览器截图验证，控制台错误和警告均为 0
* 浏览器测试用户、会话和审计记录已清理，当前用户数为 0，首次管理员引导保持开启
* `/webdav/` 的 `OPTIONS` 与 `PROPFIND` 返回 `tstokyo` 入口的 Basic 认证挑战
* `/admin-webdav/` 的 `OPTIONS` 与 `PROPFIND` 返回 `Administrator` 入口的 Basic 认证挑战
* Nginx 开机任务能够恢复 `192.168.20.54:443` 监听

## 回滚点

IIS 配置备份：`Before-Nginx-WebDAV-Gateway-20260723-115937`

Nginx 文件备份：`D:\nginx\backup\20260723-115937`
