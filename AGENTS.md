# OneOps 项目规则

## Git 仓库与分支

- 本项目唯一正式远端为 `origin`，地址为 `https://github.com/sunshaoxuan/oneops.git`。
- 正式分支为 `master`。完成测试的修改直接提交并推送至 `origin/master`。
- 除非用户在当前任务中明确要求，不得创建本地分支、远端分支或 Pull Request。
- 开始修改前应获取并核对 `origin/master`，推送后应确认本地 `HEAD` 与 `origin/master` 一致。

## 文档与版本

- 功能、数据模型、运维方式、接口或项目规则发生变化时，必须同步维护相关文档。
- 根目录 `VERSION` 是项目当前版本号。发布时同步更新 `CHANGELOG.md`、各应用 `package.json` 和画面版本号。
- 版本号采用 Semantic Versioning，正式版本使用 `v<version>` Git 标签。

## 质量与发布

- 代码、配置、文档或 UI 修改完成后必须执行相关测试。
- UI 修改必须完成构建、发布、浏览器画面检查、控制台检查和截图验证。
- 测试失败或验证不完整时，不得报告完成或推送正式版本。

## 安全与仓库边界

- 密码、令牌、私钥、证书私钥、生产环境变量、日志、备份及运行时数据不得提交。
- 源码、正式脚本、需求文档、技术说明和可公开的验证证据应纳入版本管理。
