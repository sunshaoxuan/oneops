# OneOps プロジェクト規約

## Git リポジトリとブランチ

- 本プロジェクトの正式なリモートは `origin` のみとし、URL は `https://github.com/sunshaoxuan/oneops.git` とする。
- 正式ブランチは `master` とする。テスト済みの変更は `origin/master` へ直接コミットしてプッシュする。
- ユーザーが現在のタスクで明示的に指示した場合を除き、ローカルブランチ、リモートブランチ、Pull Request を作成しない。
- 変更開始前に `origin/master` を取得して差分を確認し、プッシュ後にローカル `HEAD` と `origin/master` の一致を確認する。

## 文書とコメントの言語

- プロジェクトが作成・管理する文書、要件、調査記録、運用説明、コードコメント、スクリプトコメントは日本語で記述する。
- 中国語と英語は、画面の多言語表示文言、外部データ、テストフィクスチャ、API の固定値、第三者ライセンス原文に限って使用できる。
- 機能、データモデル、運用方法、インターフェース、プロジェクト規約を変更した場合は、関連文書も同時に更新する。

## バージョン

- ルートの `VERSION` をプロジェクトの現行バージョンとする。リリース時は `CHANGELOG.md`、各アプリケーションの `package.json`、画面表示バージョンを同期する。
- バージョン番号は Semantic Versioning に従い、正式リリースには `v<version>` の Git タグを使用する。

## 品質とリリース

- コード、設定、文書、UI を変更した場合は、関連するテストを実行する。
- UI の変更では、ビルド、リリース、ブラウザ表示、コンソール、スクリーンショットを確認する。
- テストが失敗した場合や検証が完了していない場合は、完了報告や正式リリースを行わない。

## セキュリティとリポジトリ境界

- パスワード、トークン、秘密鍵、証明書秘密鍵、本番環境変数、ログ、バックアップ、ランタイムデータをコミットしない。
- ソースコード、正式スクリプト、要件文書、技術説明、公開可能な検証証跡をバージョン管理の対象とする。
