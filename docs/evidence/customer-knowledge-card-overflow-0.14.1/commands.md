# Commands

```powershell
..\runtime\node\pnpm.cmd --filter @one-ops/portal-shell test
..\runtime\node\pnpm.cmd check
Invoke-RestMethod http://127.0.0.1:8092/api/work-center/v1/health
Invoke-RestMethod http://127.0.0.1:8093/api/work-center/v1/health
git diff --check
git status --short
```

Browser acceptance measured `.customer-knowledge-candidate` and `.customer-knowledge-json` client and scroll widths on the signed in customer page, inspected computed wrapping styles, read Console issues and captured a viewport screenshot.
