# 実行 Command

```powershell
pnpm.cmd --filter @one-ops/portal-shell test -- UserStatusSelect.test.tsx auth-ui.test.ts
pnpm.cmd --filter @one-ops/portal-shell test
pnpm.cmd --filter @one-ops/portal-shell build
```

実 Browser では対象ページを表示し、状態 Select の DOM、計算済み Style、Console 及び Screenshot を確認しました。
