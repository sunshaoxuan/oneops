# 测试结果

日期：2026-07-27

## 自动化检查

1. 定向前端测试：3 个测试文件，32 项通过。
2. 完整 `pnpm check`：108 项网关测试通过，4 项 Python 测试通过，64 项前端测试通过。
3. TypeScript 编译与 Vite 生产构建通过。
4. 构建保留现有大分块警告，没有新增编译错误。

## 发布检查

1. 发布脚本执行成功。
2. `nginx -t` 通过。
3. 网关健康状态为 `UP`。

## 浏览器检查

1. `AI設定` 展开后显示 `Model API` 与 `Agent Gateways`。
2. `Model API` 页面只显示模型接口设置。
3. `Agent Gateways` 页面只显示 Gateway 设置与新增入口。
4. 页面角色为 `tab` 的元素数量为 0。
5. 720 像素宽度下标题区为纵向布局，`scrollWidth=705`，没有横向溢出。
6. 浏览器截图已在验收会话中检查，菜单层级、间距、空状态与按钮位置正常。
7. 控制台错误和警告数量为 0。
