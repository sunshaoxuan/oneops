# 実装回执

状態: 実装、公開、検証完了

## 実装内容

1. 添付本文抽出、OCR、解析状態、再解析操作を削除した。
2. 画像、PDF、Word、Excel を上位プレビュードロワーで表示する。
3. その他形式を直接ダウンロードする。
4. PDF Range 要求、許可済み S3 リダイレクト、Cookie 分離を維持する。
5. 添付本文を AI 支援の入力対象から除外する。

## 検証

Gateway 115 件、Builder Python 4 件、Portal 72 件、TypeScript と本番ビルドが成功した。公開処理と Nginx 設定検査が成功し、公開画面で `v0.2.3`、PDF 添付のプレビュー操作、上位ドロワー、ダウンロード操作、詳細状態の維持を確認した。ブラウザーコンソールのエラーと警告は 0 件である。

## ロールバック

対象コミットを取り消して再公開する。データベース変更はない。
