# 测试结果

## 已完成

1. 后端聚焦测试：7 项通过。
2. 后端 Node 全量测试：85 项通过。
3. Worker Python 测试：4 项通过。
4. 前端测试：46 项通过。
5. 前端生产构建：通过。
6. 正式发布脚本、Nginx 配置、网关健康和 HTTPS 检查：通过。
7. 数据库存在 `ai_model_settings`；`models.settings.read` 和 `models.settings.write` 只授予 `SYSTEM_ADMIN`；生产配置记录数为 1，API Key 以加密密文保存。
8. 浏览器显示模型设计与 API 设置；提供方列表只有 OpenAI；API Key 为密码输入；虚构连接测试显示成功；控制台警告和错误为 0。
9. 1280 像素视口下，管理内容区宽 955 像素，模型设计页与设置卡片宽 907 像素，正好覆盖扣除内容区左右内边距后的全部可用宽度；页面、卡片和表单的 `max-width` 均为 `none`。
10. 表单在桌面宽度下按约 261/620 像素双栏展开；响应式单栏规则由前端测试覆盖。
11. 截图：`D:\nginx\docs\evidence\model-design-openai-settings-20260726.png`。
12. 操作区与 905 像素宽表单的右边缘同为横坐标 1209；“测试连接”为左侧辅助按钮，“保存设置”为最右侧主按钮，两按钮宽度均为 112 像素；浏览器控制台警告和错误为 0。
13. 浏览器密码输入框持有完整 25 字符虚构 API Key，输入类型为 `password`，明文显示按钮数量为 0；保存与测试请求都收到相同的完整 25 字符值，保存后仍完整回填。
14. 生产仓库读取验证返回 `apiKeyConfigured=true` 和 42 字符完整值，Endpoint 与模型均已配置；检查只输出长度和布尔状态。

## 测试边界

自动化使用本地 HTTP 桩和虚构 API Key。生产记录只检查加密状态，没有输出或记录管理员保存的 API Key 明文。临时服务和目录已删除。
