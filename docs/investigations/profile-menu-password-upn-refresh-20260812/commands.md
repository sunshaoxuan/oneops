# 実行コマンド記録

```text
Production users / auth_identities read-only query for sun.shaoxuan@onehr.jp
vitest run src/auth-ui.test.ts
tsc -b
node --test gateway/auth.test.mjs gateway/auth-controller.test.mjs gateway/identity-database.test.mjs
node --test gateway/*.test.mjs
vitest run
vite build
git diff --check
publish-portal.ps1 -SkipChecks -SkipGatewayRestart -Reason profile-menu-password-upn-refresh-c7a8661
HTTPS /api/work-center/v1/health
In-app Browser DOM / Console / Screenshot inspection
```
