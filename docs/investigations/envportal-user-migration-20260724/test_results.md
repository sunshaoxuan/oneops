# 测试结果

## 自动化

1. OneOps 网关测试 73 项通过。
2. OneOps 前端 Vitest 测试 39 项通过。
3. 生产构建通过。
4. 运维发布脚本测试通过。
5. 用户迁移专项测试 8 项通过。

## 数据

1. 初次预演为新建 11、合并 1、冲突 0、忽略 0。
2. 生产执行写入 12 名用户的迁移结果。
3. 执行后再次预演为新建 0、合并 12、冲突 0。
4. 数据库中有 12 名 Windows 域用户和 12 条迁移审计事件。

## 浏览器

1. 用户管理两页共显示 12 名用户。
2. 两名新 `OPERATOR` 和九名新 `VIEWER` 均为 `ACTIVE`。
3. 抽查 `x03025` 的角色范围为“全体（すべての組織機関）”。
4. 超级管理员保留企业邮箱、`LOCAL` 和 `WINDOWS` 身份及 `SYSTEM_ADMIN`。
5. 认证审计两页共显示 12 条 `ENVPORTAL_USER_MIGRATED`。
6. 控制台警告和错误为 0。
