# 测试结果

## 自动化测试

1. 网关问询测试 23 项通过。
2. Portal 测试 74 项通过。
3. 全量 `pnpm check` 通过，其中 Node.js 118 项、Builder Python 4 项、Portal 74 项。
4. TypeScript 检查和 Portal 生产构建通过。
5. `git diff --check` 通过。

## 关键回归

1. 没有支持回复的线程判定为 `UNANSWERED`。
2. 仅有系统事件的线程仍判定为 `UNANSWERED`。
3. 存在内部讨论或客户可见回复的线程判定为 `REPLIED`。
4. Provider 返回与系统不一致的模式时拒绝结果。
5. 未回复且需要调查时，Provider 即使返回草案，服务端仍清空草案。
6. 已回复模式缺少草案时拒绝结果。
7. 历史辅助记录缺少新增字段时仍可显示。

## 真实页面

1. 已回复重点分析显示模式标签、重点回复标签与全部分析分区。
2. 未回复样本显示调查优先状态，回复视图没有文本框。
3. 两次真实调用均显示实际 Provider 和 Token 用量。
4. 草案使用真实换行。
5. 浏览器控制台 0 条记录。

