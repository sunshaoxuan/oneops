# コマンド記録

1. `git fetch origin master`
2. `python -m unittest builder/oneops_worker_test.py`
3. `python -m py_compile builder/standalone_packager.py ...`
4. `node --test gateway/project-language.test.mjs`
5. `pnpm install --frozen-lockfile`
6. `pnpm check`
7. Help 単独構築 fixture から実制品ディレクトリを生成して内容を確認
8. `git diff --check`
