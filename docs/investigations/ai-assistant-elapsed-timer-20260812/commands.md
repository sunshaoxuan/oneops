# 実行コマンド

```text
git fetch origin master
pnpm --filter @one-ops/portal-shell test -- ai-assistant-interaction-style.test.ts GenerativeConversationLoader.test.tsx
pnpm --filter @one-ops/portal-shell build
```

