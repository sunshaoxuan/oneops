# 测试结果

## 自动化

- 网关测试：110 项通过。
- Python Worker 测试：4 项通过。
- 前端测试：67 项通过。
- TypeScript 与 Vite 生产构建：通过。
- 已有构建体积提醒继续存在，不影响本次功能。

新增覆盖：

1. 数据库映射仅在管理员设置读取模式下回填解密密码。
2. 设置 GET 接口显式请求完整凭据。
3. 问合支援页面不再把已保存密码清空。
4. 共用凭据控件默认使用 Password 输入，提供复制操作。
5. 模型 API Key 和 Agent Gateway Token 复用同一控件。

## 生产浏览器

- 问合支援设置密码：初始 `type=password`，值非空。
- 点击显示：切换为 `type=text`，值保持非空。
- 再次隐藏：恢复 `type=password`。
- 点击复制：按钮状态变为“コピーしました”。
- 模型 API Key 完成同样验证。
- 控制台 warning：0。
- 控制台 error：0。
- 验收截图保持密码掩码。
