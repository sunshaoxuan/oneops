# 测试结果

验证日期：2026-07-27

## 自动测试

1. 网关单元测试：108 项通过。
2. 問合支援聚焦测试：15 项通过。
3. 前端测试：62 项通过。
4. 构建工作进程测试：4 项通过。
5. TypeScript 与 Vite 生产构建通过。
6. 完整 `pnpm check` 通过。

## 真实网站验证

1. UPDS 登录成功。
2. OPEN 查询返回真实总件数 817，真实网站显示上限为 500。
3. 详情成功解析工单号、标题、客户、分类、状态、创建时间、更新时间、附件、初始提问和支持记录。
4. 抽样详情中的 7 条支持记录全部解析出发言人。发言人和时间位于消息头部，正文不再重复发言人与时间。
5. 公开回复和内部讨论分别标准化为 `CUSTOMER_VISIBLE_REPLY` 与 `INTERNAL_DISCUSSION`。

## AI 辅助验证

1. 查询和打开详情后，辅助任务表记录数保持为 0。
2. 点击 `AI補助` 后创建 1 条辅助任务。
3. 本次任务明确使用 `MODEL` Provider 与页面显示的实际 Model。
4. 辅助任务完成，问题分析和回复草案两个视图均可打开。
5. 回复草案输入框可编辑，复制和重新生成按钮存在。
6. 问题分析返回 4 个可定位证据入口。
7. 页面没有真实网站回复提交动作。
8. 回复草案中的转义换行会转换为编辑框内的真实换行，复制内容保持一致。
9. Provider 返回 Token 用量时保存输入、输出和总 Token；未返回时显示“未提供”。

## 系统操作审计

1. 认证事件会标记 `AUTHENTICATION` 能力与执行结果。
2. 已登录业务 API 会记录用户、会话、请求编号、能力、动作、结果、状态码和耗时。
3. 問合支援会记录查询、详情、附件和 AI 任务创建。
4. AI 任务会记录开始、完成或失败的过程事件。
5. 管理员可按操作人、能力、结果和时间范围查询。
6. 健康检查、会话轮询和实时事件不会生成高频审计记录。
7. 审计节点与用户管理平级，页面具有 4 个摘要卡、6 个筛选项及可展开结果表。
8. 实际按操作人筛选后返回匹配记录，审计查询自身进入审计。

## 发布与浏览器

1. 发布脚本执行成功。
2. `nginx -t` 通过。
3. 网关健康状态为 `UP`。
4. 浏览器控制台错误 0，警告 0。
5. 大型右侧抽屉在桌面视口按 88% 宽度显示，固定头部与独立滚动可见。
6. 脱敏截图：`inquiry-support-drawer-redacted.png`。
7. 审计工作台脱敏截图：`system-operation-audit-redacted.png`。
8. 最终发布资源为 `index-D7ySlAP_.js` 与 `index-UPhZwIkz.css`，构建目录和发布目录的 SHA-256 均一致。
9. 开始日期默认留空，结束日期默认使用浏览器本地当天 `2026-07-27`，两者均允许用户调整。
10. 列表表头显示“顧客”，实际查询返回 11 行，客户单元格均为单段文本且不包含提问人换行。
11. 日期默认值、客户列表解析与表头文案均有单元测试覆盖。
12. 900px、1500px、900px 往返调整后，页面宽度分别与视口可用宽度一致，中等窗口稳定使用双列查询布局。
13. 700px 窗口稳定使用单列查询布局，页面无横向溢出，1180px 工单表格宽度仅保留在表格内部滚动。
14. 真实工单包含 4 次客户追问。旧提取方式有 3 个问题正文包含“へのコメント”，新提取方式为 0；客户问题与支持消息的正文重复数为 0。
15. 发布后的详情抽屉中，客户问题与 7 条支持消息的正文重复数为 0，浏览器控制台错误 0、警告 0。
16. 真实网站查询表单确认内容检索字段为 `k`，扩展到追加提问、回答和评论的开关为 `cr`；No. 检索使用同一 `k` 字段及 `sbi` 开关。
17. 真实源内容查询、No. 精确查询、No. 加内容组合查询均命中预期 OPEN 工单。
18. AI 历史筛选返回 3 张 OPEN 工单，全部存在 `inquiry_assist_runs` 记录；查询前后辅助任务总数不变。
19. AI 历史按钮点击后 `aria-pressed` 为 `true`，页面显示 3 条业务结果。按钮仅切换查询条件。
20. 新查询区域在 1500、900、700 像素宽度分别为 12 轨、双列、单列，页面均无横向溢出。
21. 新发布资源为 `index-LTCqbWT5.js` 与 `index-agIXeSMD.css`。
22. 浏览器控制台错误 0、警告 0。脱敏截图为 `inquiry-support-search-filters-redacted.png`。
23. 真实 CLOSED 样本的 6 个 `td.message_body` 共有 23 个 `<br>`；新解析结果恢复客户问题与支持记录中的真实换行。
24. 抽样 5 张 `CLOSED: 評価受信` 工单，5 张均解析出评价对象、满意度和评价时间，其中 4 张包含评价评论。
25. 5 张样本中共有 9 个客户问题和 7 条支持记录保留了原始换行。
26. 发布页面打开真实 `CLOSED: 評価受信` 工单后，客户评价卡、满意度标签、评价评论和评价时间均可见。
27. 浏览器中客户问题保留 9 个换行，支持记录保留换行；两类正文和评价评论的计算样式均为 `pre-wrap`。
28. 评价卡宽 1055 像素、高 96 像素，使用浅金色边框与渐变背景；浏览器控制台错误 0、警告 0。
29. 最终发布资源为 `index-BPKqPlMd.js` 与 `index-B0M86Fbq.css`。
30. 真实详情包含客户名称、标题和正文，因此未保存或提交本轮真实工单截图，避免客户数据进入版本库。视觉结构通过浏览器计算样式和 DOM 状态验证。
31. 真实网站详情字段同时包含“緊急度”和独立的“問合せレベル”；解析器分别输出 `urgency` 与 `inquiryLevel`。
32. 定向测试为网关 17 项、问询支援前端 14 项，全部通过。完整检查为网关 112 项、Python 4 项、前端 69 项，生产构建通过。
33. 发布后的真实“至急”标题工单显示“緊急度: 至急”，徽标使用红色边框和红色渐变高警示样式。真实普通工单的源紧急度为空，画面显示“緊急度: 一般”。
34. 真实网站独立的“問合せレベル”字段继续解析到数据契约；源值为空时不进入抽屉标题，避免把模板空字段误解为有效等级。
35. 600 × 800 视口下详情抽屉的实际宽度为 600 像素，左右边界为 0 和 600，文档横向宽度为 600，详情正文独立纵向滚动。
36. 浏览器开发日志错误 0、警告 0。脱敏局部截图为 `docs/evidence/inquiry-urgency-urgent-20260727.png` 与 `docs/evidence/inquiry-urgency-normal-20260727.png`。
37. 附件不再固定返回 `NOT_PARSED`。详情读取会下载并解析 PDF、DOCX、XLSX、PPTX 和常用文本格式，解析结果通过 30 分钟内存缓存复用。
38. 文本型 PDF 夹具按页提取正文；扫描型 PDF 夹具在文本层为空时使用 Windows 日语 OCR，返回 `PARSED`、`[Page 1]`、页数和 `windows-ocr-ja-JP` 解析方式。
39. 真实 OPEN 工单 38950 的两份扫描 PDF 均由“文字情報なし”变为“解析済み”，可展开正文分别为 1269 和 1275 字符，均包含页码标记，计算样式为 `white-space: pre-wrap`。
40. 真实 XLSX 附件成功解析，达到 200,000 字符上限后显式标记为截断。AI 上下文对重复附件去重，单文件最多 40,000 字符，单次最多 100,000 字符。
41. 真实附件下载只允许从 `ss.onehr.jp` 跳转至固定的 `scientiass.s3.amazonaws.com` 存储源，来源站 Cookie 不发送到对象存储；其他重定向源继续拒绝。
42. 真实工单详情重新打开前后 `inquiry_assist_runs` 均为 2 条，附件自动解析没有创建 AI 请求。
43. 完整检查为网关 119 项、Python 4 项、前端 71 项，生产构建通过。发布后 `nginx -t` 成功，网关状态为 `UP`，浏览器控制台错误 0、警告 0。
44. 最终发布资源为 `index-CU9CaL7D.js` 与 `index-C4xupS6r.css`。脱敏附件状态截图为 `docs/evidence/inquiry-attachment-parsed-20260727.png`。

## 修正记录

1. 真实查询需要提交搜索表单的全部默认成功控件，单独传递状态只会返回摘要。
2. 客户字段的实际标签为“顧客”。
3. 初始提问正文位于 `.well_main_content td.message_body`。
4. 支持记录发言人与时间位于 `.mod_date_info`，正文位于 `td.message_body`。
5. 认证资料物理用户 ID 位于 `currentProfile.id`。
6. AI 面板每个上下文只自动创建一次任务，Provider 失败后不自动重试。
7. 搜索结果的客户单元格由“客户名、换行、提问人”组成，列表只取换行前的客户名；提问人留在详情问题区。
8. 单列 Grid 的自动轨道会被固定横向表格的固有宽度撑开；使用 `minmax(0, 1fr)` 和完整的 `min-width: 0` 收缩链隔离表格内部滚动。
9. “サポートセンターへの追加質問”只能按完整标题识别为客户追问；“追加質問へのコメント”属于支持记录容器。追问正文优先读取自身 `td.message_body`，避免把后续评论区复制进客户问题。
10. 真实网站同一登录会话并发执行多张票的搜索会返回 403。AI 历史逐票检索必须顺序执行，避免并发破坏会话。
11. 未注册的表单存储字段不会触发 `Form.useWatch` 更新。AI 历史布尔值必须作为隐藏表单项注册，保证按钮状态和提交值同步。
12. `textContent` 不会把 `<br>` 转换为换行。正文需要遍历 DOM，并把 `<br>` 与块级节点边界显式转换为文本换行。
13. CLOSED 客户评价位于 `well_response well_comment_2` 区块，字段标签为“満足度”和“コメント”，评价时间位于 `.mod_date_info`。
14. 工单等级的真实字段标签为“問合せレベル”，与“緊急度”位于不同单元格，必须使用独立数据字段。
15. 空的模板等级不应提升为标题级业务信息。标题包含“至急”时，紧急程度使用“至急”；其他工单优先使用源“緊急度”，空值归一为“一般”。
16. Ant Design 抽屉的宽度内联样式位于 `ant-drawer-content-wrapper`。移动端覆盖规则必须从 `rootClassName` 作用到该父级包装节点。
17. 真实附件下载会从来源站 302 跳转到对象存储。附件链路需要精确允许存储源，同时禁止转发来源站 Cookie。
18. 图片型 PDF 没有文本层。解析器需要先尝试文本提取，再渲染页面并调用系统日语 OCR，OCR 结果继续保留页码和换行。
19. Windows PowerShell 5.1 使用 WinRT OCR 前需要加载 `System.Runtime.WindowsRuntime`，并以 `ExecutionPolicy Bypass` 启动签入项目的 OCR 脚本。
20. 附件事件有独立渲染分支时，必须在该分支显式渲染附件列表；工单级未分组附件还需要兜底区域，避免解析成功后仍然不可见。
