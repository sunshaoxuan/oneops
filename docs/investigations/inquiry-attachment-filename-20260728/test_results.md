# 検証結果

## 自動検証

1. 添付応答ヘッダーの聚焦テスト 24 件が成功した。
2. Gateway テスト 119 件が成功した。
3. Builder Python テスト 4 件が成功した。
4. Portal テスト 80 件が成功した。
5. TypeScript と Vite の本番ビルドが成功した。
6. 生産成果物は `index-BLiq3y2t.js` と `index-BGls2fPK.css` である。
7. `git diff --check` が成功した。

## 実環境検証

1. 固定ポート 8092 の Gateway を再起動し、ヘルス応答 `UP` を確認した。
2. Chrome の既存認証セッションで OneOps の問合せ No. 38950 を開いた。
3. `六十歳到達時等賃金証明書.pdf` のプレビュー操作を実行した。
4. PDF ビューアーが 1 ページの文書本体を表示し、従来の `ERR_INVALID_CHAR` 応答がないことを確認した。
5. iframe は `mode=preview` と UTF-8 パーセント符号化した原名を含む同一オリジン URL を使用した。
6. iframe の表示寸法は 1280 x 867 px であった。
7. ブラウザーコンソールのエラーと警告は 0 件であった。
8. 実顧客情報を含むため、検証スクリーンショットはリポジトリへ保存していない。

## 公開スクリプトの初回失敗

初回の完全公開は、同時進行していた Portal URL 履歴対応が型定義を別ファイルへ移動した直後に、既存レイアウトテストが旧ファイルを参照していたため停止した。公開スクリプトは公開前の品質門で停止し、Web ルートを変更していない。その並行変更側でテスト参照が修正された後、Portal テスト 80 件の成功を再確認した。本修正は Gateway の応答ヘッダーだけを変更するため、Gateway の安全な再起動で実環境へ反映した。
