# 测试结果

日期：2026-07-27

## 自动化检查

1. 网关测试 108 项通过。
2. Python 测试 4 项通过。
3. 前端测试 65 项通过。
4. TypeScript 编译和 Vite 生产构建通过。
5. 构建保留现有大分块警告。

## 发布检查

1. 发布脚本执行成功。
2. Nginx 配置检查通过。
3. 线上 HTML 引用 `index-CpUv4UNM.js` 和 `index-BSqvtZnc.css`。

## 浏览器检查

1. 名称和启用状态位于左列，宽度均为 261 像素。
2. API Endpoint 和 Access Token 位于右列，宽度均为 620 像素。
3. Access Token 说明占用宽列，启用状态所在行不再由窄列长说明撑高。
4. 页面没有横向溢出。
5. 操作按钮紧随字段区域，主操作位于最右侧。
6. 控制台错误和警告数量为 0。
7. 验收截图保存为 `docs/evidence/agent-gateway-balanced-layout-20260727.png`。
