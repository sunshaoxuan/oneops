# コマンド記録

1. `rg` と `Get-Content` で OneOps と droneci の target validation を比較
2. `python -m unittest builder.oneops_worker_test...`
3. `python -m py_compile builder/host_standalone_console.py builder/oneops_worker_test.py`
4. `pnpm check`
5. Runtime 配信後に Browser で標準発版の前端単独入力を確認
6. Browser Console と Screenshot を確認
